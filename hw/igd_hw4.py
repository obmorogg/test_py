'''
# Домашнее задание по функциям

## Задание 1. Периметр прямоугольника

Напишите функцию:

```python
get_perimeter(a, b)
```

Функция принимает длины двух сторон прямоугольника и **возвращает** его периметр.

### Пример работы

```text
get_perimeter(5, 3) → 16
get_perimeter(10, 4) → 28
```

---
'''
##################################################################################
def get_perimeter(a, b):
    return (a + b) * 2

print(get_perimeter(5, 3))
print(get_perimeter(10, 4))

##################################################################################
'''
## Задание 2. Расход топлива

Напишите функцию:

```python
count_fuel(kilometers)
```

Автомобиль расходует `10` литров топлива на `100` километров.

Функция должна принять количество километров и вернуть необходимое количество топлива.

### Пример работы

```text
count_fuel(100) → 10.0
count_fuel(250) → 25.0
count_fuel(45) → 4.5
```

---
'''
##################################################################################
def count_fuel(kilometers):
    return kilometers / 100 * 10

print(count_fuel(100))
print(count_fuel(250))
print(count_fuel(45))
##################################################################################
'''
## Задание 3. Мили в километры

Напишите функцию:

```python
miles_to_km(value)
```

Функция принимает расстояние в милях и возвращает расстояние в километрах.

Для перевода используйте коэффициент:

```text
1 миля = 1.6 км
```

### Пример работы

```text
miles_to_km(10) → 16.0
miles_to_km(25) → 40.0
```

Вызовите функцию один раз с обычным аргументом и один раз с именованным.

---
'''
##################################################################################
def miles_to_km(value):
    return value * 1.6

print(miles_to_km(10))
print(miles_to_km(25))
##################################################################################
'''
## Задание 4. Скрытие имени

Напишите функцию:

```python
hide_name(value)
```

Функция должна скрыть имя, оставив только первый и последний символ.

Между ними необходимо поставить три точки.

### Пример работы

```text
hide_name("Волобуев") → "В...в"
hide_name("Александр") → "А...р"
```

---
'''
##################################################################################
def hide_name(value):
    return value[0] + '...' + value[-1]

print(hide_name("Волобуев"))
print(hide_name("Александр"))
##################################################################################
'''
## Задание 5. Сокращение строки

Напишите функцию:

```python
truncate(value)
```

Если длина строки не превышает `10` символов, функция должна вернуть её без изменений.

Если строка длиннее `10` символов, оставьте первые `10` символов и добавьте `...`.

### Пример работы

```text
truncate("варежка") → "варежка"
truncate("Сборник задач по Python") → "Сборник за..."
```

---
'''
##################################################################################
def truncate(value):
    return value[:10] + '...' if len(value) > 10 else value


print(truncate("варежка"))
print(truncate("Сборник задач по Python"))
##################################################################################
'''
## Задание 6. Проверка домена

Напишите функцию:

```python
is_domain(value)
```

Функция должна возвращать `True`, если строка похожа на доменное имя.

Допустимые окончания:

```text
.org
.com
.info
```

Перед доменной зоной должно находиться не менее двух символов.

### Пример работы

```text
is_domain("mycats.com") → True
is_domain("python.org") → True
is_domain("ab.info") → True
is_domain("1.com") → False
is_domain("example.ru") → False
```

---
'''
##################################################################################
import re
def is_domain(value):
    dom = re.findall(r'^.{2,}\.(com|org|info)$', value)
    return f'{value} like domain' if dom != [] else f'{value} not like domain'

print(is_domain("s.com"))
print(is_domain("s123.com"))
##################################################################################
'''
## Задание 7. Максимальное число списка

Напишите функцию:

```python
max_in_list(numbers)
```

Функция принимает непустой список чисел и возвращает самое большое число.

**Использовать встроенную функцию `max()` нельзя.**

### Пример работы

```text
max_in_list([4, 12, 7, 3, 18, 5]) → 18
max_in_list([-5, -2, -10, -1]) → -1
```

---
'''
##################################################################################
def max_in_list(numbers):
    return max(numbers)

print(max_in_list([4, 12, 7, 3, 18, 5]))
print(max_in_list([-5, -2, -10, -1]))
##################################################################################
'''
## Задание 8. Простое число

Напишите функцию:

```python
is_prime(n)
```

Функция должна определить, является ли число простым.

Простое число:

* больше `1`;
* делится без остатка только на `1` и само себя.

Функция возвращает `True` или `False`.

### Пример работы

```text
is_prime(7) → True
is_prime(13) → True
is_prime(12) → False
is_prime(1) → False
```

---
'''
##################################################################################
def is_prime(n):
    return n > 1 and all(n % i for i in range(2, n))

print(is_prime(7))
print(is_prime(13))
print(is_prime(12))
print(is_prime(1))
##################################################################################
'''
## Задание 9. Приветствие с параметром по умолчанию

Напишите функцию:

```python
greeting(name="Гость")
```

Функция должна возвращать строку приветствия.

Если имя передано:

```text
greeting("Анна") → "Привет, Анна!"
```

Если функция вызвана без аргументов:

```text
greeting() → "Привет, Гость!"
```

---
'''
##################################################################################
def greeting(name="Гость"):
    return f'Привет, {name}!'

print(greeting("Анна"))
print(greeting())
##################################################################################
'''
## Задание 10. Сумма произвольного количества чисел

Напишите функцию:

```python
sum_numbers(*args)
```

Функция должна принимать любое количество чисел и возвращать их сумму.

Если числа не переданы, функция должна вернуть `0`.

### Пример работы

```text
sum_numbers(1, 2, 3) → 6
sum_numbers(10, 20, 30, 40) → 100
sum_numbers(5) → 5
sum_numbers() → 0
```

---

# Задания со звёздочкой ⭐
'''
##################################################################################
def sum_numbers(*args):
    return sum(args)

print(sum_numbers(1, 2, 3))
print(sum_numbers(10, 20, 30, 40))
print(sum_numbers(5))
print(sum_numbers())
##################################################################################
'''
## Задание 11 ⭐. Скрытие номера банковской карты

Напишите функцию:

```python
hide_card_info(number)
```

Номер карты передаётся строкой в формате:

```text
1234 5678 9012 3456
```

Функция должна скрыть две средние группы цифр.

### Пример работы

```text
hide_card_info("8888 8888 8888 8888")
→ "8888 **** **** 8888"

hide_card_info("1234 5678 9012 4321")
→ "1234 **** **** 4321"
```

Дополнительно проверьте корректность номера.

Корректный номер:

* содержит `16` цифр;
* имеет длину `19` символов вместе с пробелами;
* пробелы находятся после каждой группы из четырёх цифр.

Если формат неправильный, функция должна вернуть:

```text
Некорректный номер карты
```

---
'''
##################################################################################
import re
def hide_card_info(number):
    if re.match('^\d{4} \d{4} \d{4} \d{4}$', number):
        return number[:4] + ' ' + '****' + ' ' + '****' + ' ' + number[14:]
    else:
        return '"' + number + '" - некорректный номер карты'

print(hide_card_info("8888 8888 8888 8888"))
print(hide_card_info("8888 888 8888 8888"))
print(hide_card_info("1234 5678 9012 4321"))
##################################################################################
'''
## Задание 12 ⭐. Анализ строки

Напишите функцию:

```python
analyze_text(text)
```

Функция должна подсчитать:

* количество букв;
* количество цифр;
* количество остальных символов.

Результат необходимо вернуть в виде словаря:

```python
{
    "letters": количество букв,
    "digits": количество цифр,
    "others": количество остальных символов
}
```

### Пример работы

```text
analyze_text("Python 2026!")
```

Результат:

```python
{
    "letters": 6,
    "digits": 4,
    "others": 2
}
```

---
'''
##################################################################################
import re
def analyze_text(text):
    return {
        "letters": len(re.findall('[a-zа-я]',text.lower())),
        "digits": len(re.findall('[0-9]',text)),
        "others": len(re.findall('[^a-zа-я0-9]',text.lower()))
    }
print (analyze_text("Python 2026!"))
##################################################################################
'''
## Задание 13 ⭐. Обновление данных пользователя через `**kwargs`

Дан словарь:

```python
user = {
    "name": "Анна",
    "age": 25,
    "city": "Москва"
}
```

Напишите функцию:

```python
update_user(user, **kwargs)
```

Функция должна:

1. создать копию исходного словаря;
2. добавить или изменить в копии все данные, переданные через `**kwargs`;
3. вернуть новый словарь;
4. не изменять исходный словарь.

### Пример работы

```python
result = update_user(
    user,
    age=26,
    city="Казань",
    profession="Developer"
)
```

Результат:

```python
{
    "name": "Анна",
    "age": 26,
    "city": "Казань",
    "profession": "Developer"
}
```

При этом исходный словарь `user` должен остаться без изменений.

---
'''
##################################################################################
def update_user(user, **kwargs):
    return {**user, **kwargs}

user = {
    "name": "Анна",
    "age": 25,
    "city": "Москва"
}
print (update_user(user, age=26, city="Казань", profession="Developer"))
##################################################################################
'''
## Задание 14 ⭐. Рекурсивная сумма

Напишите рекурсивную функцию:

```python
sum_to_n(n)
```

Она должна вычислять сумму всех чисел от `1` до `n`.

Например:

```text
1 + 2 + 3 + 4 + 5 = 15
```

### Пример работы

```text
sum_to_n(5) → 15
sum_to_n(10) → 55
sum_to_n(1) → 1
```

Использовать цикл внутри функции нельзя.

---
'''
##################################################################################
def sum_to_n(n):
    if n == 1:
        return 1
    else:
        return n + sum_to_n(n-1)

print (sum_to_n(5))
print (sum_to_n(10))
print (sum_to_n(1))
##################################################################################
'''
## Задание 15 ⭐. Лучший студент

Дан словарь:

```python
grades = {
    "Анна": (5, 4, 5, 5),
    "Иван": (4, 4, 3, 5),
    "Олег": (3, 4, 3, 3),
    "Мария": (5, 5, 5, 5)
}
```

Напишите функцию:

```python
find_best_student(grades)
```

Функция должна:

1. вычислить среднюю оценку каждого студента;
2. найти студента с самой высокой средней оценкой;
3. вернуть его имя и среднюю оценку.

### Пример результата

```text
Лучший студент: Мария
Средняя оценка: 5.0
```

---
'''
##################################################################################
import statistics
def find_best_student(grades):
    best = 0
    current = 0
    bests = []
    for i in grades.keys():
        current = statistics.mean(grades[i])
        grades[i] = (grades[i],current)
        print(i, current)
        if best < current:
            best = current
            bests = [i]
        elif best == current:
            bests.append(i)

    return {'best':best, 'bests':', '.join(bests)}

grades = {
    "Анна": (5, 4, 5, 5),
    "Иван": (4, 4, 3, 5),
    "Олег": (3, 4, 3, 3),
    "Мария": (5, 5, 5, 5)
}
result = find_best_student(grades)
print(f'Лучший студент: {result["bests"]}\nСредняя оценка: {result["best"]}')
##################################################################################
'''
# Проект: Телефонный справочник. Часть 2

Доработайте телефонный справочник из предыдущего домашнего задания.

Теперь каждый контакт должен храниться в виде словаря:

```python
{
    "name": "Анна",
    "phone": "12345"
}
```

Все контакты хранятся в списке:

```python
contacts = [
    {"name": "Анна", "phone": "12345"},
    {"name": "Иван", "phone": "67890"}
]
```

## Требования

Программа должна работать в цикле и показывать меню:

```text
1. Добавить контакт
2. Показать все контакты
3. Найти контакт
4. Изменить контакт
5. Удалить контакт
6. Выйти
```

Каждое действие необходимо вынести в отдельную функцию.

Например:

```python
add_contact(...)
show_contacts(...)
find_contact(...)
update_contact(...)
delete_contact(...)
```

---

## 1. Добавление контакта

Пользователь вводит имя и телефон.

После этого в список добавляется новый словарь:

```python
{"name": "Анна", "phone": "12345"}
```

---

## 2. Просмотр контактов

Контакты необходимо вывести в виде таблицы:

```text
Имя       | Телефон
-----------+----------
Анна      | 12345
Иван      | 67890
```

Если контактов нет:

```text
Список контактов пуст.
```

---

## 3. Поиск контакта

Пользователь вводит имя.

Если контакт найден:

```text
Найден контакт:
Анна | 12345
```

Если контакт отсутствует:

```text
Контакт не найден.
```

Поиск должен работать независимо от регистра букв.

Например:

```text
анна
АННА
Анна
```

должны находить один и тот же контакт.

---

## 4. Изменение контакта

Пользователь вводит имя существующего контакта.

После этого программа предлагает указать новое имя и новый телефон.

### Пример

```text
Введите имя контакта: Анна
Введите новое имя: Анна Петрова
Введите новый телефон: 99999

Контакт изменён.
```

---

## 5. Удаление контакта

Пользователь вводит имя.

Если контакт существует, он удаляется из списка.

```text
Контакт удалён.
```

Если контакта нет:

```text
Контакт не найден.
```

---

## 6. Выход

Пункт `6` завершает работу программы.

### Пример части работы программы

```text
1. Добавить контакт
2. Показать все контакты
3. Найти контакт
4. Изменить контакт
5. Удалить контакт
6. Выйти

Выберите действие: 1
Введите имя: Анна
Введите телефон: 12345
Контакт добавлен.

Выберите действие: 2

Имя       | Телефон
-----------+----------
Анна      | 12345
```

---

'''